{% extends 'base.html.twig' %}

{% block title %}TypeService{% endblock %}

{% block body %}
    <h1>TypeService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_service.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_service.nom }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ type_service.categorie }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_service.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_service_index') }}">back to list</a>

    <a href="{{ path('app_type_service_edit', {'id': type_service.id}) }}">edit</a>

    {{ include('type_service/_delete_form.html.twig') }}
{% endblock %}
