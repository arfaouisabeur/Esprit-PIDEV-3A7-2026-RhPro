<form method="post" action="{{ path('app_demande_service_delete', {'id': demande_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_service.id) }}">
    <button class="btn">Delete</button>
</form>
